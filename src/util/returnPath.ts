export enum ReturnPath {
  Users = "/users",
  UserId = "/users/:id",
  Products = "/products",
  ProductsId = "/products/:id",
  ProductSearch = "/product/search",
  Purchases = "/purchases",
  PurchasesId = "/purchases/:id",
  PurchasesProducts = "/purchases_products",
  PurchasesProductsId = "/purchases_products/:idpurchase/:idproduct",
}
