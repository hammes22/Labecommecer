export enum ReturnPath {
  Users = "/users",
  UserId = "/users/:id",
  UsersIdPurchases = "/users/:id/purchases",
  Products = "/products",
  ProductsId = "/products/:id",
  ProductSearch = "/product/search",
  Purchases = "/purchases",
}
